# Java Types and Concepts

## Optional<T>
- Used to represent a value that might be `null`. Helps prevent `NullPointerExceptions`.

## LocalDate
- A Java class used to represent a date without time (e.g., for task due dates).
